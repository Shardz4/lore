{
  "name": "lore-contracts",
  "version": "1.0.0",
  "description": "Hardhat deployment suite for Lore ZK ledger",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network baseSepolia"
  },
  "dependencies": {},
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
